#include <iostream>

using namespace std;

int n, m, x, y;
int a[100005];
int b[100005];

int main() {
  cin >> n >> m >> x >> y;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  for (int i = 1; i <= n; i++) {
    cin >> b[i];
  }
}
